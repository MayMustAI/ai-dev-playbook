# macOS
.DS_Store

# Editors
.vscode/
.idea/
*.swp
*.swo

# Claude Code local settings (per-user, not shared)
.claude/settings.local.json

# Logs / tmp
*.log
tmp/

# .worklogs/ 는 의도적으로 추적한다 (플러그인 규약).
# feature 브랜치에 작업당 한 장 기록 → squash merge 시 main 에 자연 축적.
# 이 디렉터리를 전체 ignore 하지 말 것. 개별 파일만 제외 필요 시 명시적으로.
